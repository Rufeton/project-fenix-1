apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
  labels:
    app: fenix-postgres
spec:
  selector:
    app: fenix-postgres
  clusterIP: None
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP