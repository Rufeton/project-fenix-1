apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: fenix-postgres
spec:
  selector:
    app: fenix-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP